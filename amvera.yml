meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  command: venv/bin/python3 manage.py migrate && venv/bin/python3 init_admin.py && gunicorn project.wsgi:application --bind 0.0.0.0:80
  containerPort: "80"
