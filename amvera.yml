meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  command: gunicorn project.wsgi:application --bind 0.0.0.0:80
  containerPort: "80"
